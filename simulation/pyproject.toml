[project]
name = "reachable-human-phenotype-sim"
version = "0.1.0"
description = "Design discrimination among embodiment theories, a causal-inference model of morphology-congruent binding, and the multiscale reachable set with hysteresis"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
]
